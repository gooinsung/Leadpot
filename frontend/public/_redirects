# Cloudflare Pages SPA 폴백: 존재하는 정적 파일(embed.js 등)은 그대로 서빙되고,
# 그 외 모든 경로는 index.html 로 넘겨 React Router 가 처리하게 한다.
# 이게 없으면 /f/{id}·/p/{slug}·/integrations 같은 딥링크와 서브도메인 라우팅이 404.
/*    /index.html   200
