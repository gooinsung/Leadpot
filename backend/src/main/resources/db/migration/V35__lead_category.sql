-- 리드 '분야' 도장(V35) — 접수 순간 폼의 분야(forms.category, V34)를 리드에 복사한다.
-- 폼에서 상속(조회 시점 계산)하면 분야를 지정/변경할 때 과거 리드까지 소급되는데,
-- 운영 요구는 "지정한 이후 접수분부터만 집계"다 → 리드에 박아 과거를 건드리지 않는다.
-- 과거 리드에 붙이고 싶으면 인박스에서 체크해 일괄 지정한다(소급은 명시적으로만).
-- 기존 행은 백필하지 않는다(전부 NULL = 분야 없음).
ALTER TABLE leads ADD COLUMN category VARCHAR(50);
