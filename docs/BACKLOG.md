# docs/BACKLOG.md — 기능 선택 체크리스트

> 전체 기능 목록이다. **`[ ]` → `[x]`로 체크하거나**, 나에게 "A1, B1, D1 … 이거 1차로" 라고 알려주면
> 1차(지금 작업)로 옮기고 나머지는 여기 백로그에 남겨둔다.
> 상세 표(난이도/외부서비스/우선순위)는 [`FEATURES.md`](FEATURES.md) 참고.

범례: ⭐ = 실서비스 최소 핵심(1차 추천) · 🔒 = 외부 유료서비스 필요

---

## 🎯 1차 작업 (지금 만들 것) — 34개 확정

> 상세 순서·Phase는 [ROADMAP.md](ROADMAP.md) 참고. 🔒 = 외부 준비물 필요.

- **계정**: A1 로그인 · A2 다계정 · A4 비번재설정
- **랜딩 제작**: B1 단순랜딩 · B2 폼항목/동의 · B3 이미지 상하배치 · B5 요소 자동배치 · B7 미리보기
- **폼 관리**: C1 디자인 커스터마이징 · C2 완료페이지/리다이렉트
- **공개·수집**: D1 공개URL+수집 · D2 커스텀 도메인 🔒
- **리드 관리**: E1 목록/상태 · E2 CSV · E3 중복관리 · E4 휴지통
- **알림**: H3 카카오 알림톡 🔒
- **마케팅**: I1 픽셀 · I3 SEO · I4 전환분석 · I5 요소 노출/클릭
- **통계**: J 방문자 정보(IP·OS·기기·유입경로) 수집·통계
- **보안·차단**: K1 SSL · K2 IP차단 · K3 중복제출방지 · K5 접근권한
- **우리 개선**: M1 폼독립·재사용 · M2 기본형폼 · M3 스텝폼 · M4 콘텐츠블록 · M5 본인인증🔒 · M6 외부임베드 · M7 유형확장구조

> 나머지 항목(아래 전체 목록에서 위에 없는 것)은 2차 이후 백로그로 유지.

---

## 📋 전체 기능 (여기서 고르기)

### A. 계정 · 인증
- [ ] **A1. 이메일 회원가입/로그인** ⭐ — 이메일·비번으로 가입하고 로그인하는 기본 기능
- [ ] **A2. 다계정·멀티 랜딩** ⭐ — 한 계정으로 랜딩페이지 여러 개 만들고 관리
- [ ] **A3. SNS 로그인** — 네이버·카카오·페북 버튼으로 간편 로그인
- [ ] **A4. 비밀번호 재설정** — 비번 잊었을 때 이메일로 재설정 링크 보내기

### B. 랜딩페이지 제작
- [ ] **B1. 단순형 랜딩 생성** ⭐ — 이미지 몇 장 + 폼 얹어서 코딩 없이 페이지 만들기
- [ ] **B2. 폼 항목 구성 / 개인정보 동의** ⭐ — 이름·연락처 등 입력칸 추가, 필수여부, 동의 체크박스
- [ ] **B3. 이미지 상/하 배치** — 폼 위·아래로 이미지 여러 장 순서대로 넣기
- [ ] **B4. 블록 빌더(고도화)** — 드래그로 요소 추가·이동, 색상·크기 편집 (가장 손 많이 감)
- [ ] **B5. 요소 자동 배치** — 이미지·폼·버튼을 알아서 예쁘게 정렬
- [ ] **B6. 업종별 템플릿** — 금융·병원·교육 등 완성형에서 시작
- [ ] **B7. 실시간 미리보기(PC/모바일)** — 만들면서 바로 화면 확인, 모바일 뷰 전환

### C. 폼 관리
- [ ] **C1. 폼 디자인 커스터마이징** — 폼 가로 크기·색상·글자 크기 바꾸기
- [ ] **C2. 제출 완료 화면/리다이렉트** — 신청 끝나면 감사 페이지 보여주거나 다른 링크로 이동

### D. 공개 페이지 · DB 수집
- [ ] **D1. 공개 URL + 폼 수집** ⭐ — 만든 페이지를 링크로 배포, 방문자가 남긴 정보를 DB에 저장 (서비스 핵심)
- [ ] **D2. 커스텀 도메인 연결** 🔒 — 내 도메인(mysite.com)으로 페이지 열기
- [ ] **D3. 사용자별 서브도메인** (나중에) — 서비스 도메인이 `lead-pot.com`이면 사용자마다 `bali.lead-pot.com`, `test.lead-pot.com` 식 서브도메인 할당. 그 사용자의 랜딩은 `bali.lead-pot.com/landing/{landingId}` 형태로 접근. (와일드카드 DNS/SSL + 서브도메인→계정 라우팅 필요, D2와 연계)

### E. DB / 리드 관리 (디비내역)
- [ ] **E1. 리드 목록/상세 + 상태관리** ⭐ — 수집된 고객 정보 보기, 신규/상담중/완료 표시
- [ ] **E2. CSV/엑셀 내보내기** ⭐ — 모은 DB를 엑셀 파일로 다운로드
- [x] **E3. 중복 데이터 관리** — 리드폼 항목별 '중복 방지(allowDuplicate)' 체크가 켜진 필드를 기준으로, 값이 겹치는 리드에 '중복' 배지 + '중복만 보기' 필터(리드 목록). 표시·필터만(삭제는 기존 휴지통 수동). 프론트 계산(추가 저장 없음).
- [ ] **E4. 휴지통(복구)** — 실수로 지운 데이터 되살리기
- [x] **E1(고도화). 리드 상세 + 메모/이력 + 태그** — 리드 상세 모달(전체 답변·방문자정보), 사용자 메모 + 상태변경 자동 이력(SYSTEM), 리드별 자유 태그(칩·목록 필터). `lead_notes`·`leads.tags`(Flyway V16).
- [x] **E5. 구글 스프레드시트 전송** — 새 리드 접수 시 구글시트로 자동 전송. 방식=**Apps Script 웹훅**(OAuth·구글 클라우드 프로젝트 불필요, 무료). 계정 설정(`/integrations`)에 웹앱 URL 입력, 리드폼별 알림 on/off. 커밋 후 비동기 best-effort. (Flyway V16 integration_settings)

### F. CRM (팀플 / Team+)
- [ ] **F1. 팀원 관리** — 팀장·담당자 등록하고 조직 구성
- [ ] **F2. DB 자동 배정** — 새 DB가 들어오면 팀원에게 설정한 수량대로 자동 분배
- [ ] **F3. 진행 상태 관리** — 상담 단계(접수→통화→완료 등) 내 맘대로 설정
- [ ] **F4. 고객 관리(메모)** — 고객별 상담 메모·이력 남기기
- [ ] **F5. 개별/다수 SMS 발송** 🔒 — 고객 골라서 문자 보내기
- [ ] **F6. 팀 통계** — 일별/월별/팀원별/단계별 실적 보기
- [ ] **F7. 외부 랜딩 API 연동** — 다른 데서 만든 랜딩의 DB도 여기로 모으기
- [ ] **F8. 휴지통 복구(고객)** — 지운 고객 정보 복구

### G. 파티 (데이터 공유)
- [ ] **G1. 계정 간 데이터 공유** — 다른 계정과 DB만 공유해서 협업

### H. 알림
- [x] **H(신규). 텔레그램 알림** — 새 리드 접수 시 텔레그램 봇 메시지(리드폼 이름·주요 답변·접수일시, 중복 의심 표시). 계정 설정(`/integrations`)에 봇 토큰·채팅ID 입력, 리드폼별 on/off. 무료.
- [ ] **H1. 이메일 알림** — 새 DB 들어오면 내 메일로 알려주기
- [ ] **H2. SMS 문자 알림** 🔒 — 새 DB 들어오면 문자로 알려주기
- [ ] **H3. 카카오 알림톡** 🔒 — 카톡으로 알림 (사업자 등록 필요)
- [ ] **H4. 앱/웹 푸시 알림** — 브라우저·폰 푸시로 알림

### I. 마케팅 · 트래킹
- [ ] **I1. 광고 픽셀 설치** — 구글·메타·틱톡·당근 광고 추적 코드 심기
- [ ] **I2. UTM 추적** — 어느 광고·경로로 들어왔는지 기록
- [ ] **I3. 검색엔진 등록(SEO)** — 구글·네이버 검색에 잘 노출되게 메타태그 세팅
- [x] **I4. 전환 분석** — 전환 퍼널(방문→폼 열기→접수, 고유 방문자 기준, 단계별 %). 통계 페이지에 퍼널 카드. (경량: 기존 방문/리드 + form_open 이벤트 활용)
- [x] **I5. 요소 노출/클릭 분석** — 오버레이 CTA(=폼 열기) + **공개 랜딩의 이미지·버튼·링크 자동 클릭 추적**. `interaction_events`(Flyway V15) + `POST /api/public/events`(best-effort) + 통계 '요소 클릭' 집계. 랜딩에 위임 클릭 핸들러(폼 내부·오버레이 CTA 제외해 중복 방지). (노출 임프레션 추적은 추후)

### J. 통계 · 분석
- [ ] **J1. 접속 통계** — 방문자 수, 기간별 추이
- [ ] **J2. 유입 경로** — 어디서(리퍼러)·어떤 기기로 왔는지
- [ ] **J3. UTM 캠페인 통계** — 캠페인별 성과 비교
- [ ] **J4. 전환율 대시보드** — 방문 대비 신청률 한눈에

### K. 보안 · 차단
- [ ] **K1. SSL 암호화** ⭐ — https 적용 (Cloudflare로 무료, 사실상 기본 포함)
- [x] **K2. IP 차단** — 리드폼별 IP/CIDR 대역 차단(사유 메모) + 차단 접속 로그 확인. 차단 시 공개 제출 거부(중립 메시지), 시도는 로그로 기록(REQUIRES_NEW 로 롤백과 무관하게 저장). Flyway V13(ip_blocks·ip_block_hits), `com.leadpot.ipblock`, 관리 UI `/forms/{id}/ip-blocks`.
- [ ] **K3. 중복 제출 방지** — 짧은 시간 반복 제출·스팸 막기
- [ ] **K4. 봇/에이전트 차단** — 자동 프로그램(봇) 제출 걸러내기
- [ ] **K5. 리드 데이터 접근 권한 통제** ⭐ — 내 DB는 나(내 팀)만 보게 하기

### L. 결제 · 요금제
- [ ] **L1. 요금제/이용권** 🔒 — 유료 플랜 만들고 랜딩별 결제
- [ ] **L2. PG 결제 연동** 🔒 — PortOne·토스로 실제 결제 받기
- [ ] **L3. 정기결제(구독)** 🔒 — 매월 자동 결제
- [ ] **L4. 메시지 크레딧** — 문자 발송용 크레딧 충전/차감

### M. 우리만의 개선 기능 (디비카트에 없거나 다르게)
- [ ] **M1. 폼 독립·재사용** ⭐ — 입력폼을 랜딩과 별도로 만들고, 하나의 폼을 여러 랜딩에서 재사용
- [ ] **M2. 기본형 입력폼(BASIC)** ⭐ — 한 화면에 항목 나열하는 일반 폼
- [ ] **M3. 선택형/스텝형 입력폼(STEP)** — 배경 블러 + 단계별 선택(질문 N/총) → 연락처 → 제출 (은오 자가진단 스타일)
- [ ] **M4. 폼 내 콘텐츠 블록 삽입** — 입력 항목 사이에 이미지·HTML·텍스트·구분선을 자유롭게 배치
- [ ] **M5. 휴대폰 본인인증 옵션** 🔒 — 폼에서 본인인증을 선택적으로 ON/OFF (외부 인증 서비스)
- [x] **M6. 폼 외부 임베드** — 만든 리드폼을 외부 사이트/블로그에 삽입. 방식=**script 직접 주입 + Shadow DOM**(iframe 아님). 임베드 코드 `<div data-leadpot-form="{id}"></div><script src=".../embed.js" async></script>` → embed.js가 컨테이너에 Shadow root 만들고 기존 React `PublicFormView` 마운트(대상 사이트 CSS와 격리). 공개 API(`/api/public/**`)는 모든 오리진 CORS 허용. 퍼가기 코드는 리드 목록 페이지에서 복사. 검증: 가짜 외부 오리진에서 렌더·격리·교차출처 제출→리드 저장 확인.
- [ ] **M7. 확장 가능한 폼 유형 구조** — 새 폼 유형(팝업/챗형 등)을 렌더러 추가만으로 확장
- [x] **M8. 재사용 HTML 요소 라이브러리 (요소 생성기)** — 정적 요소 1단계 완료. 미리 만든 HTML 조각(헤더·푸터·CTA 등)을 저장해두고, 랜딩·리드폼의 HTML 블록에 꺼내 삽입해 재사용.
  - 관리: `html_components`(owner_id·name·category·html, Flyway V14) + CRUD `/api/html-components`(본인만 K5) + 관리 페이지 `/html-components`(목록/생성/편집, 원본 HTML textarea + 미리보기). 동의문서 패턴.
  - 삽입: **복사 삽입(스냅샷)** — 랜딩/폼 HTML 블록 편집기의 `HtmlComponentPicker` 셀렉트에서 요소 선택 → 그 시점 HTML을 블록에 이어붙임(이후 라이브러리 수정은 이미 삽입된 곳에 영향 없음).
  - 결정(사용자 §0): 삽입=복사 스냅샷 / 사용 위치=랜딩+폼 둘 다 / 편집=원본 HTML textarea. 고정/플로팅은 요소 HTML의 `style` position 으로 처리.
  - **2단계 완료**: 동적 요소 — 랜딩 HTML 블록의 '동적 요소 삽입' 셀렉트로 삽입. **실시간 신청 수**·**남은 자리(목표−현재)**·**최근 신청자 토스트(우측 상단, 이름 마스킹)**·**플로팅 헤더/푸터**. 공개 랜딩(LandingView)이 `data-lp-live` 마커를 `GET /api/public/landings/{id}/live`(연결폼 활성 리드 수 + 최근 마스킹) 데이터로 하이드레이션. 브라우저 검증 완료.

---

## 📝 사용법

1. 위 목록에서 지금 만들 것을 고른다 (체크 또는 ID로 알려주기).
2. 고른 항목은 상단 **🎯 1차 작업** 섹션으로 옮기고, 그것만으로 상세 실행 계획을 짠다.
3. 나머지는 이 백로그에 그대로 두고, 나중에 착수할 때 꺼내 쓴다.
